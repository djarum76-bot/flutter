import 'package:flutter/material.dart';
import 'package:inapp/src/app.dart';

void main() {
  runApp(MagicWeatherFlutter());
}

class MyApp extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Text("asdasd"),
      ),
    );
  }
}